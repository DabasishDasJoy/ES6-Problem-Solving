// Output the product of three numbers using arrow function
const productOfNumbers = (num1, num2, num3) => num1 * num2 * num3;

console.log(productOfNumbers(2, 5, 3));

// Print three lines using templete string/ backtick

const str = `I am a developer.
I love to code.
I love to eat biriyani.
`;
console.log(str);

//Write an arrow function with two parameter one is from argument and another is default then return the summution

const sum = (a, b=5) => a + b;

console.log(sum(2));